import type { FunctionComponent } from "react";
import classes from "./ClockIcon.module.css";

const ClockIcon: FunctionComponent = () => (
  <svg className={classes["gg-time"]} />
);

export default ClockIcon;
